<local:TabBase x:Class="TimetablingWPF.DataClassTabItem"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TimetablingWPF"
      mc:Ignorable="d">

    <TabItem.Resources>
        <MenuItem x:Key="miEditItem" Header="Edit item"/>
        <MenuItem x:Key="miNewItem" Header="New item"/>
        <MenuItem x:Key="miDeleteItem" Header="Delete selection"/>
        <MenuItem x:Key="miDuplicateItem" Header="Duplicate item"/>
        <Style x:Key="tbStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
        <DataTemplate x:Key="NameTemplate">
            <TextBlock Style="{StaticResource tbStyle}" Text="{Binding Name}"/>
        </DataTemplate>
    </TabItem.Resources>
    <StackPanel Orientation="Vertical" Name="spMain">
        <ToolBarTray>
            <ToolBar Height="30">
                <ToolBar.Resources>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToolBar.Resources>
                <Button Name="btNewToolbar" Command="local:DataGridCommands.NewItem">
                    <Image Source="{StaticResource PlusIcon}"/>
                </Button>
                <Button Name="btEditToolbar" Command="local:DataGridCommands.EditItem">
                    <Image Source="{StaticResource EditAccountIcon}"/>
                </Button>
                <Button Name="btDuplicateToolbar" Command="local:DataGridCommands.DuplicateItem">
                    <Image Source="{StaticResource DuplicateIcon}"/>
                </Button>
                <Button x:Name="btDeleteToolbar" Command="local:DataGridCommands.DeleteItem">
                    <Image Source="{StaticResource DeleteBinIcon}"/>
                </Button>
            </ToolBar>
        </ToolBarTray>
        <StackPanel Orientation="Horizontal" Name="spFilter" Visibility="Collapsed">
            <TextBlock Style="{StaticResource BlackDialogText}" Text="Name:"/>
            <TextBox x:Name="filterName" Width="50" VerticalContentAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
        <DataGrid Name="dgMainDataGrid" HeadersVisibility="Column" IsReadOnly="True" AutoGenerateColumns="False"  CanUserResizeColumns="True" CanUserResizeRows="True"
                          SelectionUnit="FullRow" SelectionMode="Extended" CanUserSortColumns="True">
            <DataGrid.ContextMenu>
                <ContextMenu Name="cmContextMenu">
                    <StaticResource ResourceKey="miNewItem"/>
                    <Separator/>
                    <StaticResource ResourceKey="miEditItem"/>
                    <StaticResource ResourceKey="miDuplicateItem"/>
                    <Separator/>
                    <StaticResource ResourceKey="miDeleteItem"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </StackPanel>
</local:TabBase>
